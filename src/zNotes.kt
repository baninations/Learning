// Labels
// Arrays can contain 4 different operations. Search(n), Access(1), Insert(n),Delete(n)
// for(i in 0..10 -1) is the same as, for(i in 0 until 9)
// .trim() in strings means to remove any spaces before and/or after the string
// Use hash map for a fast query